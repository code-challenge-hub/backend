name: ci-cd-dev

on:
  pull_request:
    branches:
#      - develop
      - chore/ci-github-actions
  push:
    branches:
#      - develop
      - chore/ci-github-actions

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4
      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'
      # 실행 권한 부여
      - name: Grant execute permission for gradlew
        run: chmod +x gradlew
      # Gradle 빌드
      - name: Execute Gradle build
        run: ./gradlew clean build
      # Upload
      - name: upload AWS
        if: github.event_name == 'push' # Push 때 만
        run: echo "upload AWS!"
